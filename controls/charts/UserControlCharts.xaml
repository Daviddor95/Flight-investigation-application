<UserControl x:Class="WpfCharts.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfCharts"
             mc:Ignorable="d" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             d:DesignHeight="396" d:DesignWidth="519">
    <Grid>
        <lvc:CartesianChart x:Name="chart6" Series="{Binding SeriesCollectionChart6}" LegendLocation="Left" Margin="247,0,0,175">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Time" Labels="{Binding LabelsChart67}" FontSize="14"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis MaxValue="{Binding MaxValueChart6}" MinValue="{Binding MinValueChart6}" 
                          MinRange="{Binding MinRangeChart6}" MaxRange="{Binding MaxRangeChart6}"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</UserControl>
